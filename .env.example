# Supabase — client-side (safe to expose)
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

SUPABASE_AUTH_EXTERNAL_GOOGLE_CLIENT_ID=
SUPABASE_AUTH_EXTERNAL_GOOGLE_CLIENT_SECRET=

# VAPID public key — safe to expose; used by the browser to register push subscriptions.
# Generate with: npx web-push generate-vapid-keys
VITE_VAPID_PUBLIC_KEY=

# The following are set in Supabase secrets only — never use on the client
# supabase secrets set GEMINI_API_KEY=...
GEMINI_API_KEY=

# supabase secrets set VAPID_PUBLIC_KEY=... VAPID_PRIVATE_KEY=... VAPID_SUBJECT=...
VAPID_PUBLIC_KEY=
VAPID_PRIVATE_KEY=
VAPID_SUBJECT=mailto:you@example.com
